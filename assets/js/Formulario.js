export class Formulario {
    constructor(saludo) {
        this.saludo = saludo;
    }

    static validar(input) {
        // Coleccion de todos los dataset
        const tipoDeInput = input.dataset.tipo;

        if (input.validity.valid) {
        
        }

    }
}